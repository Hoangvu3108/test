//yeah
#include <stdio.h>
typedef struct Node {
	int id ;//identifi of current element
	struct Node* next;// point4er to the next element of the current element
}Node;
Node* makeNode(int id){
	node* p =(Node*)malloc(sizeof(Node));
	p->id=id; p->next=NULL;
	return p;
}
Node* insert last(int id)
int main(){
	node* h=NULL;
	dor(int id=1;i<10;id++)
	h= insertLast(id,h);
	printlist(h);
}
